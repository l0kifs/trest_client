[pytest]
log_cli = 1
log_cli_level = WARNING
log_cli_format = %(asctime)s %(levelname)8s [%(module)s] %(message)s
log_cli_date_format = %H:%M:%S

env_override_existing_values = 1
env_files =
    .env

addopts =
    --alluredir=./allure-results
    --capture=tee-sys
;    --tb=line
